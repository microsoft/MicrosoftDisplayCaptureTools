<Window
    x:Class="CaptureCardViewer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CaptureCardViewer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    Title="Frame Debugger"
    Width="1200"
    Height="600"
    ui:ThemeManager.IsThemeAware="True"
    Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Grid.ColumnSpan="3">
            <MenuItem Header="_File">
                <MenuItem Header="_Clear configuration" />
                <MenuItem Header="_Open device configuration..." />
                <MenuItem Header="_Save configuration" />
                <Separator />
                <MenuItem Header="Open test case..." />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_Documentation" />
                <MenuItem Header="_About" />
            </MenuItem>
        </Menu>

        <StackPanel
            x:Name="SPanel"
            Grid.Row="1"
            Grid.Column="0"
            Margin="10"
            Orientation="Vertical">
            <Label
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Content="Predicted Image" />
            <Image
                x:Name="PredImage"
                MinHeight="200"
                Stretch="Uniform" />
            <Button
                x:Name="lF"
                Margin="0,26"
                Click="loadFramework"
                Content="Load Framework" />

            <Label Content="CONFIGURATIONS" />

            <Grid>
                <Grid.Resources>
                    <Style BasedOn="{StaticResource {x:Type Label}}" TargetType="Label">
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="HorizontalAlignment" Value="Left" />
                        <Setter Property="Margin" Value="3" />
                    </Style>
                    <Style BasedOn="{StaticResource {x:Type ComboBox}}" TargetType="ComboBox">
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                        <Setter Property="Margin" Value="3" />
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Label Content="Refresh rate" />
                <Label Grid.Row="1" Content="Resolution Rate" />
                <Label Grid.Row="2" Content="Color" />
                <ComboBox
                    x:Name="cbi_ref"
                    Grid.Column="1"
                    SelectionChanged="configurations">
                    <ComboBoxItem Content="60hz" />
                    <ComboBoxItem Content="75hz" />
                </ComboBox>


                <ComboBox
                    x:Name="cbi_res"
                    Grid.Row="1"
                    Grid.Column="1"
                    SelectionChanged="configurations">
                    <ComboBoxItem Content="1920x1080" />
                    <ComboBoxItem Content="720x800" />
                </ComboBox>

                <ComboBox
                    x:Name="cbi_col"
                    Grid.Row="2"
                    Grid.Column="1"
                    SelectionChanged="configurations">
                    <ComboBoxItem Content="Blue" />
                    <ComboBoxItem Content="Black" />
                    <ComboBoxItem Content="Green" />
                    <ComboBoxItem Content="Red" />
                    <ComboBoxItem Content="Black" />
                </ComboBox>

            </Grid>

        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Margin="10"
            Orientation="Vertical">

            <Label
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Content="Actual Image" />

            <Image
                x:Name="CaptImage"
                MinHeight="200"
                Stretch="Uniform" />

            <Button
                x:Name="dP"
                Margin="0,26"
                Click="displayProperties"
                Content="Display Properties" />

            <StackPanel Orientation="Horizontal">
                <Menu>
                    <MenuItem Header="Load Framework Components">
                        <MenuItem Click="Tlbx_Click" Header="Toolbox" />
                        <MenuItem Click="CapPlgn_Click" Header="Capture Plugin" />
                        <MenuItem Click="DispMan_Click" Header="Display Manager" />
                    </MenuItem>
                </Menu>
            </StackPanel>
        </StackPanel>

        <TabControl
            Grid.Row="1"
            Grid.Column="2"
            Margin="10"
            VerticalAlignment="Stretch">
            <TabItem Header="Log">
                <DockPanel LastChildFill="True">
                    <ui:CommandBar DockPanel.Dock="Top">
                        <ui:AppBarButton Command="{Binding Path=Logger.SaveLogCommand}" Icon="Save" />
                        <ui:AppBarButton Command="{Binding Path=Logger.ClearLogCommand}" Icon="Clear" />
                    </ui:CommandBar>
                    <FlowDocumentScrollViewer Document="{Binding Logger.Document}" />
                </DockPanel>
            </TabItem>
            <TabItem Header="Frame Properties">
                <StackPanel Orientation="Vertical">
                    <Label
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Content="Properties" />
                    <TextBox
                        x:Name="TextBlock"
                        Height="200"
                        MinHeight="200" />
                    <Button
                        x:Name="cF"
                        Margin="0,26"
                        Click="compareFrames_Click"
                        Content="Compare Frames" />
                </StackPanel>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
